import type {Metadata} from "next"; import {Suspense} from "react"; import Header from "@/components/layout/Header"; import Footer from "@/components/layout/Footer"; import SearchResults from "@/components/search/SearchResults"; import {getAllOfferings,getDestinations} from "@/lib/services/destinations";
export const metadata:Metadata={title:"Search travel experiences",description:"Search demo activities, day trips and packages across twelve African destinations."};
export default function SearchPage(){return <div className="min-h-screen bg-ink text-cream"><Header/><main className="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8"><p className="font-mono text-xs uppercase tracking-widest text-gold">120 demo listings</p><h1 className="mt-3 font-display text-4xl font-semibold sm:text-5xl">Find your next experience</h1><p className="mt-4 text-sand">Search and compare sample inventory. No availability is live.</p><div className="mt-10"><Suspense fallback={<div className="h-96 animate-pulse rounded-2xl bg-surface"/>}><SearchResults offerings={getAllOfferings()} countries={getDestinations().map(x=>x.name)}/></Suspense></div></main><Footer/></div>}
