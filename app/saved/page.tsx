import Header from "@/components/layout/Header"; import Footer from "@/components/layout/Footer"; import SavedListings from "@/components/destinations/SavedListings"; import {getAllOfferings} from "@/lib/services/destinations";
export default function SavedPage(){return <div className="min-h-screen bg-ink text-cream"><Header/><main className="mx-auto max-w-7xl px-4 py-14 sm:px-6 lg:px-8"><p className="font-mono text-xs uppercase tracking-widest text-gold">Local favourites</p><h1 className="mt-3 font-display text-4xl font-semibold">Saved listings</h1><p className="mt-4 mb-10 text-sand">Saved on this browser only. Favourites do not sync across devices.</p><SavedListings offerings={getAllOfferings()}/></main><Footer/></div>}
