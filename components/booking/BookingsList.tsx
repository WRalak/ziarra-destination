"use client";
import Link from "next/link"; import {useEffect,useState} from "react"; import {formatCurrency} from "@/lib/booking/pricing"; import {getDemoBookings} from "@/lib/booking/storage"; import type {DemoBooking} from "@/types/booking";
export default function BookingsList(){const [items,setItems]=useState<DemoBooking[]>([]);useEffect(()=>setItems(getDemoBookings()),[]);const today=new Date().toISOString().split("T")[0];const upcoming=items.filter(x=>x.date>=today),past=items.filter(x=>x.date<today);return <div className="space-y-12">{[["Upcoming demo bookings",upcoming],["Past demo bookings",past]].map(([title,list])=><section key={title as string}><h2 className="font-display text-2xl font-semibold">{title as string}</h2>{(list as DemoBooking[]).length?<div className="mt-5 grid gap-4">{(list as DemoBooking[]).map(item=><article key={item.reference} className="rounded-2xl border border-line bg-surface p-5"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><span className="font-mono text-xs text-gold">{item.reference}</span><h3 className="mt-2 font-semibold">{item.offeringTitle}</h3><p className="mt-1 text-sm text-sand">{item.date} · {item.country} · {formatCurrency(item.total,item.currency)}</p></div><div className="flex items-center gap-3"><span className="rounded-full bg-green-500/15 px-3 py-1 text-xs text-green-300">{item.status}</span><Link href={`/bookings/${item.reference}`} className="rounded-xl border border-lineStrong px-4 py-2 text-sm font-semibold text-gold">View details</Link></div></div></article>)}</div>:<p className="mt-4 rounded-xl border border-dashed border-lineStrong p-6 text-sand">No {String(title).toLowerCase()} yet.</p>}</section>)}</div>}

