export default function BookingStepper({step}:{step:number}){const labels=["Details","Traveller","Review","Payment","Confirmation"];return <nav aria-label="Booking progress"><ol className="grid grid-cols-5 gap-1">{labels.map((label,index)=><li key={label} aria-current={step===index+1?"step":undefined} className="text-center"><span className={`mx-auto grid h-8 w-8 place-items-center rounded-full text-xs font-bold ${step>=index+1?"bg-gold text-ink":"bg-surface2 text-sand"}`}>{index+1}</span><span className="mt-2 hidden text-xs text-sand sm:block">{label}</span></li>)}</ol></nav>}

