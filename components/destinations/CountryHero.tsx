import Image from "next/image";
import type { Destination } from "@/types/destination";
export default function CountryHero({destination}:{destination:Destination}){return <section className="relative min-h-[440px] overflow-hidden border-b border-line sm:min-h-[520px]"><Image src={destination.heroImage} alt={`Scenic travel landscape in ${destination.name}`} fill priority sizes="100vw" className="object-cover"/><div className="absolute inset-0 bg-gradient-to-t from-ink via-ink/45 to-ink/10"/><div className="relative mx-auto flex min-h-[440px] max-w-7xl flex-col justify-end px-4 pb-10 sm:min-h-[520px] sm:px-6 sm:pb-14 lg:px-8"><span className="text-5xl sm:text-6xl" role="img" aria-label={`${destination.name} flag`}>{destination.flag}</span><p className="mt-4 font-mono text-xs uppercase tracking-[.2em] text-gold sm:mt-5">{destination.code} · {destination.region}</p><h1 className="mt-3 break-words font-display text-4xl font-semibold min-[400px]:text-5xl sm:text-7xl">Explore {destination.name}</h1><p className="mt-4 max-w-2xl text-sm leading-6 text-cream/85 sm:mt-5 sm:text-lg sm:leading-7">{destination.description}</p></div></section>}
