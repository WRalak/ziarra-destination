import Image from "next/image";
import type { Destination } from "@/types/destination";
export default function CountryHero({destination}:{destination:Destination}){return <section className="relative min-h-[520px] overflow-hidden border-b border-line"><Image src={destination.heroImage} alt={`Scenic travel landscape in ${destination.name}`} fill priority sizes="100vw" className="object-cover"/><div className="absolute inset-0 bg-gradient-to-t from-ink via-ink/45 to-ink/10"/><div className="relative mx-auto flex min-h-[520px] max-w-7xl flex-col justify-end px-4 pb-14 sm:px-6 lg:px-8"><span className="text-6xl" role="img" aria-label={`${destination.name} flag`}>{destination.flag}</span><p className="mt-5 font-mono text-xs uppercase tracking-[.2em] text-gold">{destination.code} · {destination.region}</p><h1 className="mt-3 font-display text-5xl font-semibold sm:text-7xl">Explore {destination.name}</h1><p className="mt-5 max-w-2xl text-base leading-7 text-cream/85 sm:text-lg">{destination.description}</p></div></section>}

